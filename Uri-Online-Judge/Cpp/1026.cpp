#include <bits/stdc++.h>

using namespace std;

int main(){

    ios::sync_with_stdio(false); cin.tie(0);
    unsigned int x,y,xor;

    while(cin >> x >> y){
        xor = x^y;
        cout << xor << endl;
    }

    return 0;
}
